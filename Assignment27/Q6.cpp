/*
6. Create a class CString to represent a string.
a) Overload the + operator to concatenate two strings.
b) == to compare 2 strings.
*/
#include<iostream>
using namespace std;
class Cstring
{
    private:
           string name;
           int size=0;
    public:
           Cstring(strcpy(name1,prashant))
           {
              strcpy(name,name1);
           }
           

    
};
